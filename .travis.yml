language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm run build
deploy:
  provider: surge
  skip_cleanup: true
  domain: same-alley.surge.sh
  local_dir: build
on:
  branch: master